export type Student = {
    id: string;
    matricula: string;
    nombre: string;
    email: string;
    carrera: string;
    semestre: number;
    activo: boolean;
};

export type Course ={
    id: string;
};

export type Enrollment = {

};